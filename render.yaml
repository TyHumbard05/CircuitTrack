services:
  - type: web
    name: circuittrack
    runtime: docker
    plan: free
    dockerfilePath: ./CircuitTrack/Dockerfile
    dockerContext: ./CircuitTrack
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: AUTO_SEED
        value: "true"
