# Local development uses SQLite automatically if DATABASE_URL is omitted.
# Production: paste the Neon PostgreSQL connection string into Render as DATABASE_URL.
DATABASE_URL=sqlite:///./circuittrack.db
AUTO_SEED=true
